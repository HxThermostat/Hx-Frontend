mutation RenameLocation($input: RenameLocationInput!) {
  renameLocation(input: $input) {
    __typename
    ... on RenameLocationSuccess {
      location {
        ...LocationFields
      }
    }
  }
}
