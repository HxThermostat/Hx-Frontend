import "expo-asset";
import { AppRegistry } from 'react-native';
import "react-native-gesture-handler";
import App from './App';
// Register the app with 'main' as the name
AppRegistry.registerComponent('main', () => App);
